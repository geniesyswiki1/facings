{
  "name": "compound-intelligence-stripe",
  "private": true,
  "version": "0.1.0",
  "description": "Stripe integration for Compound Intelligence: subscription checkout, entitlement webhooks and the plan catalogue.",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "seed": "tsx scripts/seed-catalogue.ts",
    "typecheck": "tsc --noEmit",
    "listen": "stripe listen --forward-to localhost:8888/api/stripe-webhook"
  },
  "dependencies": {
    "stripe": "^22.6.0"
  },
  "devDependencies": {
    "@netlify/functions": "^4.2.6",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
